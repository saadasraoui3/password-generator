{
    "buildCommand": "composer install && npm install && npm run build && php artisan optimize",
    "outputDirectory": "public",
    "functions": {
        "api/index.php": {
            "runtime": "vercel-php@0.6.0"
        }
    },
    "routes": [{ "src": "/(.*)", "dest": "public/index.php" }]
}
